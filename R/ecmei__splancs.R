
emei__splancs = function( p, dat, pa, phi=NULL, nu=NULL ) {
  #\\ this is the core engine of emei .. localised space (no-time) modelling interpolation 

  
  
  return( list( predictions=pa, emei_stats=emei_stats ) )  
}

